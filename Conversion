//Linked list -> array
Integer[] num = list.toArray(new Integer[list.size()]);

//String -> char array
char[] chr = s.toCharArray();

//Array -> HashSet
Set<T> mySet = new HashSet<T>(Arrays.asList(someArray));

//String array -> String
String str = Arrays.toString(arr);

//Int array -> String array
String s = Arrays.toString(nums);
String str[]=s.substring(1,a.length()-1).split(", ");

//char -> int
Character.getNumericValue(c);

//int -> char
int a = 1;
char b = (char)(a + '0');

// convert array to LinkedList
LinkedList ll = new LinkedList(Arrays.asList(sa));

//How to remove an element from string
String a = "cool"
a = a.replace("o","");
a -> cl;

//Loop through HashMap based on key
for (Element key : map.keySet()) 
